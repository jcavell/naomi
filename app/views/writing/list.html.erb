<div class="jumbotron p-3 p-md-5 text-white rounded bg-dark">
  <div class="col-md-6 px-0">
    <h1 class="display-4 font-italic">Writing</h1>
  </div>
</div>

<ul>
  <% @writings.each do |writing| %>
    <li>
      <%= link_to writing.title, '/writing/' + writing.uri %>
    </li>
  <% end %>
</ul>