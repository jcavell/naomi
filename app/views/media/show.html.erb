<div class="jumbotron p-3 p-md-5 text-white rounded bg-dark">
  <div class="col-md-6 px-0">
    <h1 class="display-4 font-italic">Media - <%= @media.title %></h1>
  </div>
</div>

<% if @media.screenshot %>
  <img src = "https:<%= @media.screenshot.image_url %>"/>
<% end %>

<%= raw(@media_body) %>